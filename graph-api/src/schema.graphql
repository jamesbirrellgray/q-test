scalar Url

type HomePage {
  heading: String
  subheading: String
  heroImageUrl: Url
}

type Faq {
  title: String
  body: String
}

type Post {
  title: String
}

type Query {
  HomePage: HomePage
  FaqsPage: [Faq]
  Posts: [Post]
}

